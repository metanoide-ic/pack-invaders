# obrigatorios
pillow>=10.0          # desenha as animacoes e o texto
imageio-ffmpeg>=0.4.9 # traz o ffmpeg junto, sem instalar nada a parte

# opcional: edicao por IA (voce escreve o pedido em portugues)
# anthropic>=0.40

# opcional: legendas automaticas (transcricao local, offline)
# faster-whisper>=1.0.0

# opcional: para rodar os testes
# pytest>=7.0
